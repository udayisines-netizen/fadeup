# FadeUp mobile — configuration d'exécution (copier vers .env.local).
# La clé anon est PUBLIQUE par construction (elle vit dans chaque bundle) ;
# sa valeur courante est `ANON_KEY` dans infra/supabase/.env.
EXPO_PUBLIC_SUPABASE_URL=https://fade-up.com
EXPO_PUBLIC_SUPABASE_ANON_KEY=

// LOT 5 owns the actual marketing landing page content — this is a minimal
// placeholder that already reads as on-brand (tokens, typography) rather
// than a bare scaffold.
export function HomePage() {
  return (
    <main className="flex min-h-svh flex-col items-center justify-center gap-2 bg-paper-50 p-8 text-center">
      <h1 className="text-3xl font-semibold text-balance text-ink-950">FadeUp</h1>
      <p className="text-ink-500">The operating system for modern barbershops.</p>
    </main>
  )
}
